#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;
void xuly(vector<int> &matran, int a)
{
    sort(matran.begin(), matran.end());
    for (int i = 0; i < a; i++)
    {
        int check = 0;
        for (int j = i + 1; j < a; j++)
        {
            if (matran[i] == matran[j])
            {
                ++check;
                break;
            }
        }
        if (check == 0)
        {
            cout << matran[i] << " ";
        }
    }
}
void nhap(vector<int> &matran, int a)
{
    for (int i = 0; i < a; i++)
    {
        cin >> matran[i];
    }
}
int main()
{
    int a;
    cin >> a;
    vector<int> matran(a);
    nhap(matran, a);
    xuly(matran, a);
}
