#include <iostream>
#include <iomanip>
using namespace std;
int main()
{
    double a;
    cin >> a;
    double sum = 0;
    for (double i = 1; i <=a; i++)
    {
        sum += (1 / i);
    }
    cout << fixed << setprecision(4) << sum;
}
